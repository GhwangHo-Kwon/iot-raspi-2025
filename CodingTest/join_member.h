#ifndef JOIN_MEMBER_H
#define JOIN_MEMBER_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void Menu();
void joinMember();
void userFile(char*, char*);
void userLogin();
int idSearch(char*);

#endif